Create a compact monochrome ASCII illustration of this scene.

Current story context:
{story_context}

Last complete sentence to illustrate:
{focus_sentence}

Render the scene as ASCII art only, no preamble, explanation, labels, or markdown.
- Use only plain ASCII characters.
- The rendered content must contain no prose, no story text, and no labels.
- Produce a fixed-size ASCII canvas of exactly {height} lines.
- Each output line must be exactly {width} characters.
- Use this last complete sentence to define the immediate scene composition.
- Keep continuity consistent with the full story context.
- Use spaces for empty/background pixels.
- Keep the output readable in a terminal.
- Preserve leading spaces and do not trim indentation.
- Do not output tabs.
- Do not use backticks or code-fence formatting.
- Return all lines as raw text only; no preamble, no trailing/leading explanations, no formatting hints.

Return only the ASCII image.
